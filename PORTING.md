# 移植到新语言

> [English](PORTING_EN.md) — 本文（中文）为正本；两者不一致时以本文为准。

把 GeoToolCN 移植到一门新语言大约需要 **600 行代码**，外加一个适配器。本文说明怎么做，以及怎么证明你做对了。

已有实现可作底本：

| 语言 | 位置 | 行数（不含注释） |
|------|------|-----------------|
| Python | `GeoToolCN/_gtc.py` + `core.py` | ~600 |
| JavaScript | `packages/node/src/` | ~600 |
| Go | `packages/go/*.go` | ~700 |

## 为什么这么短

所有昂贵的工作都在构建期做完了。`.gtc` 里带着一张预计算的网格索引：**约 77% 的查询直接命中查找表，零几何运算**；其余的平均只需对 2 个多边形做射线法判定。

因此你**不需要** R-tree、GEOS、任何几何库，也不需要绑定原生代码。需要的只是：读二进制、二分查找、射线法。

## 步骤

### 1. 读 SPEC.md

`SPEC.md` 是唯一真源。§4 定义二进制格式，§2 定义 API 行为，§3 定义那些不写清就会跨语言漂移的规则。

**尤其注意 §3.1 取整**：Python 的 `round()` 是 banker's rounding，JS 的 `Math.round` 是 half-up，两者都不符合规范。

### 2. 实现读取器

按 §4 解析 `.gtc`。顺序建议：

1. 文件头 + 节表（32 字节头，每节 24 字节）
2. META（区划元数据）+ NAMES（UTF-8 字符串池）
3. 网格索引（区县 + 省级两套）
4. GEOM 惰性解码

**三个最容易踩的坑：**

- **计数用纯 varint，坐标用 zigzag varint。**（§4.5）对计数也做 zigzag 会让环数翻倍、解码越界。原型阶段就是这么挂的。
- **整型表可能未对齐。** 文件内偏移不保证按 4 字节对齐，直接把字节重解释为 u32 数组在多数语言里会崩。复制出来即可，约 50 万条目只需 1 毫秒。
- **几何要惰性解码。** 预先解析全部约 100 万个顶点要花几秒；而大多数查询根本用不到几何。

### 3. 实现 API

按 §2 逐个实现。核心是 `reverse`：

```
命中区县 → 由区县 adcode 推导 province 与 city
未命中   → 查省级网格；city 仅在直辖市/特别行政区时由 province 派生
```

**不要对三个图层各做一次独立的点在多边形判定。** 数据源本身在多处自相矛盾 —— 加格达奇区行政属黑龙江却落在内蒙古的省级多边形内；若干区县多边形越出本省轮廓延伸到离岛。独立判定会产出自相矛盾的结果。

### 4. 写适配器

放在 `conformance/adapters/<lang>.<ext>`。协议极简：stdin 每行一个 JSON 请求，stdout 每行一个 JSON 响应。

```
-> {"op":"reverse","args":[39.9042,116.4074]}
<- {"ok":["110000","110100","110101"]}
<- {"error":"invalid adcode"}
```

需要支持的 `op`：`reverse`、`lookup_adcode`、`search`、`is_in_china`、`is_in_region`、`distance`、`tree_sha256`，以及六个坐标转换函数（op 名用蛇形，如 `wgs84_to_gcj02`）。

⚠️ 序列化时小心「空值省略」：Go 的 `omitempty`、某些语言的默认行为会吞掉 `false` 和空数组，让 `is_in_china=false` 变成字段缺失。

### 5. 跑一致性测试

```bash
python conformance/run.py --adapter cmd --cmd "<你的适配器命令>"
```

35,000+ 条用例，**必须全过**。失败会按标签分组，因为移植出错通常是错一整类输入（边界、直辖市），而不是零散的点。

## 怎么证明你做对了

「通过了」还不够 —— **一套什么都不测的套件也会通过**。请故意注入错误，确认套件抓得到：

| 注入的错误 | 三个已有实现上的实测结果 |
|-----------|----------------------|
| zigzag 解码错位 | 失败 1,828 条 |
| 网格 `floor` 改 `round` | 失败 560 条 |
| 子串匹配改前缀匹配 | 失败 177 条 |
| 搜索结果排序反向 | 失败 70 条 |

若你的实现能通过其中任何一项变异，说明套件没有覆盖到你的那部分代码 —— 请告诉我们，这是套件的缺口。

**已知抓不到的两类**（不必担心）：

- 射线法 `<` 改 `<=`：只影响正好落在边上的点，SPEC §3.3 明确声明不在此处断言。
- GCJ-02 常量精度截断：输出变化小于 0.1 毫米，低于判定容差。**常量正确性只能靠代码评审**，请逐字符比对 SPEC §2.11 的表格。

## 最容易对不上的一处：行政区划树哈希

树的 JSON 形状必须**严格**匹配（SPEC §1.3）：

- 省级、市级节点：`children` **始终存在**，无下级时为 `[]`
- 区县（叶子）：`children` **不存在**，不是 `[]` 也不是 `null`

Go 的 `json:"children,omitempty"` 会把空数组一并丢掉，需要自定义序列化区分「nil」与「空但非 nil」。

哈希 = 规范化 JSON（键名升序、无空格、不转义非 ASCII）的 UTF-8 字节的 SHA-256。Go 的 `encoding/json` 默认转义 `<`、`>`、`&`，需要 `SetEscapeHTML(false)`。

## 准入清单

要把实现并入主仓并在官方文档中列出：

```
□ 实现 SPEC §2 的完整 API 契约
□ conformance 35,000+ 条用例 100% 通过
□ 适配器放在 conformance/adapters/
□ 在 .github/workflows/test.yml 的 conformance-matrix 中加一行
□ 该语言生态惯用的单元测试（不替代一致性测试）
□ README 说明安装方式
□ 零运行时依赖（这是本项目的核心承诺）
```

**不要求**你理解全部设计 —— 跑通一致性测试即客观证明实现正确。

## 数据

`.gtc` 尽量不按语言重复提交（6 MB × 语言数会累积）。构建后复制：

```bash
python pipeline/build_gtc.py            # 需要 geopandas（仅构建期）
cp GeoToolCN/data/china.full.gtc packages/<你的语言>/data/
```

**是否加进 `.gitignore`，取决于你那个生态怎么分发：**

- **有中心化 registry**（PyPI、npm、crates.io、Maven…）：加进 `.gitignore`。数据在打包时
  才注入构件，仓库里存一份就够。
- **直接从 git 解析**（Go）：**必须提交**。`go get` 只能拿到 git 里的内容，`go:embed`
  也无法引用模块目录之外的文件。`packages/go/data/china.full.gtc` 因此是仓库里唯一
  一份重复存放的数据。

判断方法：把 `git ls-files packages/<你的语言>` 的内容单独解出来，看能不能构建。

## 有问题

开 issue：https://github.com/13Cohen/GeoToolCN/issues

如果 SPEC 有含糊之处，那是 SPEC 的 bug，不是你的 —— 请报告，我们会写清楚。这份规范里的每一条精确表述，都是因为某个实现在那里踩过坑。
